name: First Workflow Project in GitHub Actions
on: 
  push:
    branches:
    - main

jobs:
  example-job:
  
    runs-on: ubuntu-latest

    steps:
      - name: Print Welcome Message
        run: echo "Welcome to our First GitHub Actions Workflow!"
